let defaultBackgroundColor = "hsl(240, 100%, 3%)"

let setBackgroundColor = (element) => {
    console.log(element)
    element.style.backgroundColor = defaultBackgroundColor;
}

export default setBackgroundColor;